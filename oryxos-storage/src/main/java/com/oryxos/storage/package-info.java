/**
 * OryxOS Storage — 持久化层.
 *
 * <p>SQLite + Spring Data JPA 持久化，包含：
 *
 * <ul>
 *   <li>SessionRepository — 会话存储
 *   <li>ToolInvocationRepository — Tool 调用审计
 *   <li>LlmCallRepository — LLM 调用审计
 * </ul>
 */
package com.oryxos.storage;
