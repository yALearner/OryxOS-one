# OryxOS — 对话提示词记录

## 1
帮我清楚项目的九个模块

## 2
不用备份帮我删除

## 3
根据项目当前docs目录下的四个文档 以及claude.md 帮我用maven初始化这个项目，按照文档中的9个模块，并能通过编译打包 只初始化模块不要写业务代码

## 4
boot目录现在能不能 java -jar运行了吗？ 是一个main函数

## 5
oryxos 的cli 需要一个main函数，他要能运行 打印oryxos的版本信息

## 6
帮我画一个oryxos的logo,放在readme,用svg格式，自由发挥 我需要一个logo

## 7
帮我在优化一下这个logo 在简介大方一点 建议去掉下面文字

## 8
logo再小一点 并且左边的图像换一个简介大方的

## 9
帮我将logo放到readme.md中

## 10
能帮我再次优化logo将图案和文件结合起来

## 11
给我打印出我前面给你的所有的提示词，放到prompt目录下面

## 12
将这些提示词打印到D:\myproject\OryxOS-one\docs\prompt目录下 01.md中这是当前全部的提示词 帮我生成一个可以让loop编程理解的提示词

## 13
/speckit-clarify —— 对 specs/001-react-runtime/spec.md 澄清 5 个问题并写入（截断语义、白名单配置位置与 fail-closed、CLI user 标识、http_get 非 2xx 语义、SC-004 验证方式）

## 14
/speckit-plan —— 生成实施计划：决策依赖升级 Boot 3.3.5→3.5.16 + Spring AI 1.0.0-M4→1.1.8 GA（移除 Alibaba）、研究验证（SQLite+JPA、Logback JSON、GA @Tool API）、产出 plan/research/data-model/contracts/quickstart

## 15
/speckit-tasks —— 生成 tasks.md（69 个任务，按 4 个用户故事分阶段）

## 16
/speckit-analyze —— 三工件一致性分析：12 个发现（0 CRITICAL）全部修复（含新增 T069 Provider 路由测试、CLAUDE.md 补 SQLite created_at 注记、spec 分支名统一）

## 17
我已经在本地安装了jdk21 和maven 帮我分析一下当前项目是否已经适配只分析不写代码 —— 结论：JDK 21 Temurin + Maven 3.9.16 已装但 PATH/JAVA_HOME 未配置；骨架编译实测通过；pom 仍是旧版本线（T001 未执行）

## 18
当前不需要 将上述的建议执行的步骤加到T001中 这样可以在之后实施阶段可以直接执行 —— 环境准备步骤（export JAVA_HOME/PATH、mvn -version 验证、阿里云镜像说明）已写入 tasks.md T001 与 Notes

## 19
更新提示词到 01.md —— 本文件更新：补录本次会话提示词 + 刷新 Loop 编程提示词到当前状态

## 20
是的（继续处理工作区未提交改动）—— 验证 GA 依赖线（mvn clean package + verify 全门禁通过）、vite override 构建验证，拆 3 个提交（build/build(website)/docs）并推送

## 21
推送 —— origin 为 https 且凭证属他人账号（Onlookers1）被 403，改用 SSH 推送 upstream；随后将 origin 也改为 SSH 地址，一劳永逸

## 22
（粘贴 Deploy to GitHub Pages 失败日志）—— 诊断：动作版本 Node 20 弃用告警（升级 upload-pages-artifact v3→v5、deploy-pages v4→v5 消除）；根因是仓库 Pages 未启用（API has_pages=false）

## 23
我下载了Github CLI Actions插件 —— gh 未在系统 PATH 中找到，指导走浏览器方案

## 24
https://github.com/yALearner/OryxOS-one/settings/pages 打开是404 —— 判定浏览器登录的不是 yALearner（settings 对无管理权限账号返回 404）

## 25
我已经重新更换了github账号了 帮我将本地代码推上去 然后重新验证一下 —— 本地与远端已同步无新改动；HTTPS push 卡 GCM 弹窗 2 分钟超时，将 origin 改为 SSH 后 Everything up-to-date；Pages 仍 404 未启用

## 26
保存好了 —— API 复验 has_pages 仍 false，保存未生效

## 27
（粘贴 Pages 设置页内容，Source 显示 Branch）—— 指出下拉框仍是 "Deploy from a branch"，需切换

## 28
我选择完GitHub Actions 会出现Custom domain……应该输入什么 —— Custom domain 留空即可（默认 yalearner.github.io），此时 has_pages=true 已生效

## 29
可以访问 但是没有ui就是简单的文字输出 —— 根因：VitePress base: '/' 导致 /assets 资源 404；改 base: '/OryxOS-one/' + Home.vue 架构图 withBase()，部署后资源全 200

## 30
是已经修好了 我已经访问到我的界面了 帮我重新设计一logo 用深色系 并且将整个UI换位深色系 显得高级一点 —— Logo 四版重设计（渐变环+底座弧线+微光）；全站 force-dark 深色调色板（#0b0f19 系）；Home.vue 深色化；13 张 SVG 架构图浅色调色板整体重映射为深色等价色

## 31
帮我将页面上的 Get-Started Architecture GitHub 等按钮修复好 现在点击跳转都是404 —— 两个根因：自定义组件硬编码 href 不会被 VitePress 加 base（改 withBase()）；GitHub 按钮指向占位符 your-org/oryxos（替换为 yALearner/OryxOS-one，共 6 处）

## 32
帮我将上述的提示语更新到提示语文档中 —— 本文件更新：补录本次会话提示词 + 刷新 Loop 编程提示词到当前状态

## 33
/oryx-spec 002-react —— oryx-spec 六阶段全流程执行（specify→clarify→plan→tasks→analyze→implement 全闭环）；G2 门禁拍板两个跨模块摩擦点（LlmGateway 依赖倒置端口 / ToolSchemaAdapter 迁 core）；28 任务全完成，mvn clean verify 全绿（47 tests + 全部静态门禁）

## 34
先合并001 在拉002 —— PR #1 已 MERGED（merge commit af0fe10）；002-react 提交 ccd7fdd 并 rebase 到合并后的 main、推送 origin、开 PR #2

## 35
帮我开始人工Dome验证 帮我罗列一下具体验证步骤 —— 编写临时验收 harness（ReActDemoManualIT：@SpringBootTest 真装配 + 真 SQLite 落库 + 真 DeepSeek + 临时 http_get/wttr.in）；输出六步验证清单（环境/跑法/四段证据/落库核对/code review/清理）

## 36
PowerShell 报错：export 无法识别 —— 验证命令 PowerShell 化：$env:JAVA_HOME/PATH 语法替代 export

## 37
PowerShell 报错：折行粘贴 ParserError —— mvn 参数必须一行写完（空值参数带引号 "-Dtest.excludedGroups="）

## 38
（Demo 真实跑通输出）—— 人工 Demo 一通过：3 轮 LLM 思考 + 2 次 http_get + 中文穿搭建议；llm_calls=3 / tool_invocations=2 真实落库；三项人工验收全部完成；顺带暴露并修复 OryxOsApplication JPA 扫描根缺口（scanBasePackages 不作用于 JPA 扫描，补 @EnableJpaRepositories/@EntityScan，fix 提交 bb80bf2）

## 39
人工验收的是哪三项 —— 回顾收尾 DoD 剩余人工项：① Demo 一真模型跑通 ② code review（循环自实现）③ oryxos.db 审计核对

## 40
我现在想要review生成的代码 帮我输出一份002需求review的流程 —— 产出 docs/reviews/002-react-review.md（六段式 review 指南，沿用 001-provider-review 先例：全景调用链/逐文件梳理/重点清单七条/刻意留白/建议顺序/验收状态）

## 41
帮我将人工验证的方法也沉淀为文档保存到项目中 —— 新增 .claude/skills/oryx-spec/references/manual-acceptance.md（临时 harness 八要点模板 + 六步标准流程 + 本机八坑对照表）；gates.md 收尾 DoD 第 6 条挂引用

## 42
帮我提交项目中当前修改的全部的代码 —— 提交 6f3cfd3（docs/流程类 7 文件：manual-acceptance、002 review 指南、001 review 先例、oryx-design 技能、requirements-convention 修订）并推送 PR #2

## 43
帮我合并 —— PR #2 合并（merge commit 836e740）；本地 main fast-forward；002-react 全生命周期闭环（六阶段→28 任务→DoD 七项→人工验收三项→review 指南→方法沉淀→合并）

## 44
帮我将上述的提示语更新到本地 —— 本文件更新：补录本次会话提示词（33~44）+ 刷新 Loop 编程提示词到当前状态

## 45
帮我查看一下当前002分支是否已经合入了 —— 确认：PR #2 MERGED（836e740），002-react 三个提交全部在 main

## 46
/oryx-spec（003-cli 六阶段全流程）—— 指定课件路径 D:\code\oryxos\docs\class\第18节；设计阶段拍板：课件 profiles/ 目录冲突按宪法 IV 取 agents/、10 项为实现级明确全接受；G2 六条全过；软停点补列 JpaScanConfigurationTest；S5 修 F1 放行 C1；29 任务全完成

## 47
我重新在终端设置了apikey 可以重新测试一把 完了在人工测试 —— 人工验证两轮：首轮暴露 Windows GBK 终端中文乱码 + 401（旧 key 无效）；诊断 key 有效性与编码根因

## 48
（java -version + chat 运行输出贴出）—— 乱码 bug 实锤：CliChannel 硬编码 UTF-8 读 GBK 终端；修复为跟随 console charset

## 49
（chat 401 输出贴出）—— key 无效诊断：直测 curl 方法与 DeepSeek 控制台重生成指引

## 50
已经结束了继续 —— 进程释放 jar 锁后重新打包（其间踩到 Defender 瞬时锁，等待重试即解）

## 51
（chat 成功输出 + session list 贴出）—— 人工验收全部通过：中文正常、会话内历史复述、跨进程恢复铁证（新进程复述上一进程的乱码消息与 /quit）、session list 两条会话时间戳正确、坑九 Found 3 仓储

## 52
帮我将刚刚人工验证的流程沉淀为文档保存 其次将上述提示语更新 添加这次代码review的逻辑 最后在需要提交 + 推送 + 开 PR #3 —— quickstart 人工验证段补全实跑流程与五坑表；docs/reviews/003-cli-review.md 六段式 review 指南；本文件补录 45~52 + Loop 刷新；提交推送开 PR

## 53
帮我将代码提交合并进主干 —— 工作区仅 4 行空白噪声（经确认还原）；PR #3 已 MERGED（merge commit af4e610）；security 门禁红核实为存量问题：spring-core 6.2.19 七个 CVE（最高 9.8）暂无修复版本（Boot 3.5.16 已是最新 3.5.x），PR #1/#2 亦带红门禁合并；本地 main fast-forward

## 54
帮我开个issue跟踪 —— issue #4：CVE 清单 + NVD_API_KEY secret 待办 + OSS Index 凭证 + 升级观察项

## 55
开始帮我生成 Notify 模块方案 —— /oryx-spec 004-notify 编号解析三层不命中（需求文档不存在）→ 规范路径转 /oryx-design notify；实测：课件第 19 节全文 + 技术方案 §6.8/§493 + 代码现状（oryxos-tool 仅 Sandbox 接口四件套、ToolExecutor 注入空 Map、Profile 无 notify 字段）

## 56
（拍板：全局注册表 + CRUD 归 Web 节）—— 课件"notify_channels 是 AGENT.md frontmatter 字段"与技术方案 §6.8"SQLite 全局注册表"冲突 → 拍板按技术方案（课程第 31 节已拍板过同一方向）；CRUD REST 端点归 Web Service 节；产出 docs/requirements/004-notify.md 六段式（7 FR + 3 NFR、坑十 enforce 先于 send、坑十一 5xx 不吞）

## 57
帮我对比19节课件有啥不足 —— 输出 A/B/C 三级不足清单 10 条：A 级三条（Profile 字段版已被否决、resolveNotifyChannel 方法不存在、@Tool 形态 vs OryxTool 抽象）；B 级四条（时序锚点过时、缺存储层内容、依赖装配、审计机制只提原则）；C 级三条（默认渠道模糊、报错形态、包名 io/com）

## 58
这个不足对比哪种方案好点 —— 全局注册表版 vs Profile 字段版五维对比（安全/共享/职责分离/运营闭环/生长点），注册表版胜出；#2 是 #1 连带结论（注册表版用 NotifyChannelRegistry 按名查表，ProfileContext 不参与）

## 59
不用改19节课件 只用维护好当前的项目中的方案 这也是我未来的课件 并且在之后的模块实现过程中 遇到上述类似的冲突 一定需要我来决定 并且你得给出两种方案当前的优缺点 —— 新规则两条固化：① 课件只读不回写，docs/requirements/ 设计文档即未来课件 ② 冲突必须停下用户拍板且附两方案优缺点 → 记忆 courseware-conflict-protocol.md + oryx-design SKILL.md 三处（§0 课件定位、§2 冲突纪律、description）

## 60
当前方案还有哪些不足吗？ —— 自审出 2 设计缺口（adapter 选择职责未守住"只加实现类不改调用方"承诺——技术方案 §6.8 原文"解析适配器和 URL"、默认渠道"升序第一条"有推错群风险）+ 5 留白（errcode 200 陷阱/4xx 重试语义/长度上限/审计不带渠道名/RestClient 超时）

## 61
（澄清：三个选项中哪种从长久性能/运行稳定性看更好）—— 性能三方案无差别（Map 查找纳秒级 vs 网络 I/O 百毫秒级）；稳定性上 adapter 显式映射方案最优（跨节契约最小、Registry 纯数据单一变更理由、呼应宪法 III）；拍板：adapter 选 2（NotifyTools 持装配处显式 Map<channelType, adapter>）+ 默认渠道=注册表恰好一条才缺省；004-notify.md 全文更新（FR-5/6/7 四步顺序、骨架、验收、人工、留白 5 条、修订说明⑤⑥）

## 62
产看之前的课件都有流程图 在我之前的方案中也根据方案生成流程图添加进方案 便于之后理解 —— 摸清仓库双版本约定（docs/images 浅色 + website/public/images 深色，文档引用 website 路径）；产出 docs-notify-flow.svg 两版并插入 004 功能需求（FR 表后、代码骨架前）

## 63
帮我将之前三个的方案也生成对应的流程图 一定要方案描述对齐 —— 001/002/003 各一张（docs-provider-flow/docs-react-flow/docs-cli-flow）×2 版本；内容逐条对齐 FR/坑对号/拍板/宪法（显式映射坑一、回环箭头坑三、轻重分流坑九等）；插入各设计文档 FR 表后；与技术方案既有图互补不覆盖

## 64
将我上面生成流程图的要求写进设计方案的skill里面 —— oryx-design SKILL.md 三处（description、步骤 5 六段成文、步骤 6 完成报告）+ template.md 新增「四、方案流程图约定」（总原则：图与方案描述逐条对齐；产出与落位；内容要素 7 条；风格基准），原通用纪律顺延为五

## 65
帮我将今天的提示语更新到本地 —— 本次更新：补录会话 53~65 + Loop 提示词刷新到当前状态

## 66
/oryx-spec 004-notify 全流程执行 —— 六阶段 S0~S6 全绿（G1~G4 门禁、软停点比对、19 任务、DoD 七项、81 tests + 全静态门禁）；分支 004-notify 自 main 新建；交付后复盘 review-analysis.md（偏差 D1~D6、不足 S1~S4/E1/P1~P2、D3 深析"无 @Component 是唯一解"、代码风格决策 §六：不引 Lombok + 模块即层已进 CLAUDE.md）；人工验收闭环：真实企业微信群机器人收到中文消息——途中 40008 实锤（课件 body {"content"} 被企微判 invalid message type）→ 方案 A 改通用 text 格式 msgtype/text/content（企微+钉钉共用、飞书归扩展）；harness 两坑修复（.oryxos 父目录先建、@DynamicPropertySource 数据源隔离）；curl 中文乱码=Windows 控制台编码与实现无关；提交两笔（b54095f docs / d4c0689 feat）+ PR #5 合并（fe6422d）+ 本地 main 同步 + 沉淀 docs/reviews/004-notify-review.md（六段式 review 指南）

## 67
帮我将第四部分代码review流程帮我沉淀 —— 产出 docs/reviews/004-notify-review.md 六段式（全景调用链/逐文件梳理/重点清单七条按风险/刻意留白八条/建议顺序五步/验收状态含剩余人工项）；本文件补录会话 66~67

## 68
开始输出tool模块的方案 —— /oryx-design 005-tool：简报拍板（编号 005-tool + 范围 ToolRegistry/六件/MCP/方式三/004 接线 + **Sandbox 无实现期间方案 A 临时放行 PermissiveSandbox**）；四文档+课件第 20 节实读；设计期自审 8 条修入（安全窗口纪律/004 遗留补验/方式三执行契约/注册过滤语义/参数规格表/平台假设）；流程图 docs-tool-system-flow.svg 双版本（避开技术方案既有 docs-tool-flow.svg）

## 69
/oryx-spec 005-tool 全流程执行 —— 六阶段 S0~S6 全绿（G1~G4、软停点比对、25 任务、DoD 七项）；S5 发现 3 MEDIUM + 2 LOW 已修（ShellTools 超时构造注入、HttpGetTool/HttpPostTool 两顶层类、T012 引用笔误等）；S6 实施 60 tool 测试 + 120 全仓测试；SpotBugs 10 项门禁修复（抑制注解按 001/004 先例 + CRLF 参数净化）；MCP SDK 0.18.3 API 全核实（McpClient.sync/StdioClientTransport/ServerParameters.builder）

## 70
帮我罗列人工验证步骤 + 帮我先分析一下 —— 六步人工验证清单；随后 PowerShell 实跑暴露三个缺陷连环修复：① ShellTools bash 不在 PowerShell PATH → 三级解析（PATH→where git→标准路径）+ 子进程 PATH 富化（coreutils 可见）② tool list 占位轻命令未接 Registry → 重命令化改造 ③ 方式三 MethodToolCallbackProvider 缺失 → 补 spring-ai-autoconfigure-model-tool 依赖后 H3 实测 1.1.8 不提供自动扫描 → **显式装配落地**（扫描 @Tool beans 构建 Provider）

## 71
人工验证闭环 —— tool list 7 工具（用户实跑确认）；方式三 echo_tool 扫描可见（临时 Bean 验证后删除）；**004 遗留 notify 补验**：chat 自动调 notify → 企业微信群收到 + 完整 ReAct 日志铁证（004 flow-status 待办闭环）；**天气 Demo**：前两版 prompt 被 DeepSeek 拒绝（"无法联网"）→ 三件套强化（具体示例+能力断言+负面禁止）后成功——http_get 失败重试 4 轮拿到西安真实天气；MCP 失联实机（WARN 带名 + SDK 20s 超时/reactor 噪音记录）；temperature 0.3 + 三件套沉淀进 oryx-design template §六

## 72
帮我遗留自查项 + 生成review流程 + 提示语更新 + 提交合并 —— 004 接口中立性自查保持遗留（用户思维练习）；产出 docs/reviews/005-tool-review.md 六段式（全景/逐文件/重点七条/刻意留白八条/建议顺序/验收状态含剩余待办）；本文件补录 68~72 + 待开发清单第 20 节标记 ✅

---

## Loop 编程专用提示词（AI Agent 上下文注入）

> 以下提示词可直接注入给 AI 编程 Loop（如 Claude Code / Cursor / Copilot），使其快速理解 OryxOS 项目全貌并继续开发。

```markdown
# OryxOS 项目上下文

## 项目定位
OryxOS 是用 Java 21 + Spring Boot 3.5 实现的面向企业场景的 Agent OS（Agent 统一底座）。
装在企业自己的 K8s 或服务器上，作为统一底座运行多个业务 Agent，共享渠道接入、模型路由、
工具调用、记忆系统、沙箱执行能力。数据完全留在企业自己的基础设施，不锁任何云生态。
Agent 在 OryxOS 上是**配置出来的，不是写代码写出来的**。

## 当前阶段
**核心阶段（pre-alpha）第 1~2 周**：核心能力一（001-provider）、核心能力二（002-react）、
CLI 与 Session 持久化（003-cli）均已交付、人工验收并合并（PR #1 af0fe10、PR #2 836e740、
PR #3 af4e610）。
新序列 spec-kit 流程（`/oryx-spec <编号>` + `docs/requirements/NNN-slug.md` 六段式需求文档）已稳定：
001-provider、002-react（28 任务）、003-cli（29 任务）全绿交付；人工验收方法论沉淀于
`.claude/skills/oryx-spec/references/manual-acceptance.md`，每课 review 指南在 `docs/reviews/NNN-review.md`。
**004-notify 设计文档已产出**（`docs/requirements/004-notify.md` 六段式 + docs-notify-flow.svg 流程图）：
拍板结论——通知渠道走 SQLite 全局注册表（notify_channels 表，AGENT.md frontmatter 不含此字段）、
CRUD REST 归 Web Service 节、NotifyTools 持装配处显式 Map&lt;channelType, adapter&gt; 按 type 选
（Registry 纯数据）、缺省渠道 = 注册表恰好一条才允许；坑十（enforce 先于 send）、坑十一（5xx 不吞）。
待人工复核后 `/oryx-spec 004-notify` 六阶段开工。
下一节按课件分节推进：第 19 节 Notify（004 设计文档已就绪）、第 20 节 Tool 体系（ToolRegistry +
内置 HttpTools + 各工具 execute 首行接 Sandbox.enforce，**就位后补跑 Demo 一完整版**）、
第 21/22 节 Memory、第 23/24 节 Sandbox 实现、第 25 节 Scheduler、第 26 节 Web。

## 9 个 Maven 模块（✅ = 已有交付内容）

| 模块 | 职责 |
|------|------|
| `oryxos-core` ✅ | 引擎本体已交付：ReActLoop、PromptBuilder、ToolExecutor、AgentService、ProfileContext、ContextLoader、Session、SessionManager、LlmGateway（依赖倒置端口）、ToolSchemaAdapter（自 provider 迁入）；抽象：OryxTool、Profile/ProfileLoader/ProfileRegistry、Message、ToolResult、JsonSchema |
| `oryxos-provider` ✅ | ProviderService（implements LlmGateway）、ProviderConfiguration（显式映射 Map<name, ChatModel>）、ProviderProperties、ProviderNotFoundException |
| `oryxos-memory` | MemoryService 统一门面，LongTermMemory 可插拔后端（待第 21/22 节） |
| `oryxos-tool` ✅ | Sandbox 接口墙已交付（Sandbox/SandboxAction/ActionType/SandboxViolationException）；内置 Tool、ToolRegistry 待第 20 节 |
| `oryxos-channel-cli` ✅ | CliChannel（读—转交—打印交互循环；终端编码跟随 console charset） |
| `oryxos-web` | REST API（10 个端点）、GlobalExceptionHandler、OpenAPI（待第 26 节） |
| `oryxos-storage` ✅ | LlmCall/ToolInvocation/SessionEntity + 三个 Repository；手工 schema.sql（llm_calls + tool_invocations + sessions 三张表，ddl-auto: none；spring.sql.init.mode=always 启动执行） |
| `oryxos-cli` ✅ | Picocli 入口 OryxOsCli 挂 12 子命令（4 分组父 + 12 叶命令）、CliAgentConfiguration（重命令装配）、轻命令零 Spring 秒回 |
| `oryxos-boot` ✅ | OryxOsApplication（scanBasePackages="com.oryxos" + **@EnableJpaRepositories/@EntityScan("com.oryxos.storage")**——坑九）；打包 mainClass=com.oryxos.cli.OryxOsCli（单二进制唯一入口，cli 插件 repackage skip） |

**依赖方向**：storage 被 core 与能力层依赖；provider/memory/tool 依赖 core（core 不反向依赖，
跨模块消费走 core 定义的端口接口——LlmGateway 先例）；web/channel-cli 依赖所有能力层；
cli 组装所有模块；boot 做依赖聚合。

## 关键设计决策（不可违背，宪法 9 条）

1. **ReAct Loop 自实现**：不用 Spring AI 的 Agent 抽象，自己写核心循环
2. **Spring AI 只用两件事**：LLM Provider 协议转换 + @Tool JSON Schema 生成。
   禁用自动 tool 执行（`ToolCallingChatOptions.internalToolExecutionEnabled(false)`，
   调度完全由 ReActLoop + ToolExecutor 控制；代码禁用 ChatClient）
3. **Provider 显式映射**：Map<String, ChatModel>，不靠类型扫描；core 消费能力层一律走 core 定义的
   端口接口（LlmGateway 先例：签名与 ProviderService.chat 逐字一致，装配时以实例注入）
4. **一个目录 = 一个 Agent**：.oryxos/agents/<name>/AGENT.md（frontmatter + 正文）；
   Skill 以软连接绑定、渐进式披露，frontmatter 不声明 skills:
5. **审计 Day One**：tool_invocations + llm_calls 两张表从核心阶段就写入 SQLite
6. **同步执行模型**：全程同步阻塞 + Java 21 Virtual Thread，不引入 Reactor/WebFlux
7. **不用 SecurityManager**：Sandbox 接口先行（enforce(SandboxAction)），
   核心阶段 WhitelistSandbox 三层白名单
8. **三种触发源共用一个引擎**：CLI/Web/Scheduler → AgentService.process() → ReActLoop
9. **Tool 模块三合一**：内置 Tool + MCP Client + Sandbox + NotifyTools 合并在 oryxos-tool

## 技术栈（GA 版本线，2026-08 已定）
- Java 21（Temurin 21.0.12.1）+ Spring Boot **3.5.16** + Maven **3.9.16**（多模块）
- Spring AI **1.1.8 GA**：官方 `spring-ai-starter-model-openai` 对接 DeepSeek/Kimi
  （base-url 不带 /v1：DeepSeek `https://api.deepseek.com`、Kimi `https://api.moonshot.cn`）。
  **spring-ai-alibaba 已移除**（无 OpenAI 兼容 starter，仅 DashScope；未来需要 Qwen 再按 GA 线引入）
- oryxos-core 引入 **spring-ai-model**（仅数据模型 Prompt/ChatResponse/ToolCall/ToolDefinition——
  002 拍板③：可用纯数据模型，禁用 Agent 抽象与自动 tool 执行；不引入 starter/自动配置）
- Picocli 4.7.6（CLI）、SnakeYAML 2.2（配置）、SQLite + Spring Data JPA（审计）
- `hibernate-community-dialects`（SQLiteDialect）+ `spring.sql.init.mode=always` 手动 schema.sql
- Logback 1.5.x + `logstash-logback-encoder:8.0`（结构化 JSON 日志）
- 核心阶段无需 PostgreSQL、Redis 等外部依赖

## 实施环境（重要，本机已实测）
- JDK：`C:\Program Files\Eclipse Adoptium\jdk-21.0.12.101-hotspot`
- Maven：`D:\tools\apache-maven-3.9.16`
- **PATH/JAVA_HOME 未配置**——每次构建前先执行：
  `export JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-21.0.12.101-hotspot"`
  `export PATH="$JAVA_HOME/bin:/d/tools/apache-maven-3.9.16/bin:$PATH"`，再 `mvn -version` 验证
- `~/.m2/settings.xml`：阿里云镜像代理 central（实测可达）；GA 依赖全在 central
- Maven wrapper 缺 mvnw 脚本，直接用本地 mvn

## 工作区结构（运行时）
```
.oryxos/
├── agents/        # 每个子目录 = 一个 Agent（AGENT.md + skills/软连接 + scripts/）
├── skills/        # 公共 Skill 实体库
├── memory/
│   └── MEMORY.md  # 长期记忆
├── sessions/      # 会话数据（已迁入 SQLite，此目录备用）
├── logs/          # 结构化 JSON 日志（oryxos.jsonl）
├── config.yaml    # 全局配置（sandbox.http.allowed_domains；Agent 级声明覆盖全局，两级未配置 fail-closed）
├── oryxos.db      # SQLite（created_at 存 ISO-8601 TEXT）
└── AGENTS.md / SOUL.md / USER.md  # Bootstrap 文件
```

## 当前已完成的工作
- [x] Maven 多模块项目初始化（9 个模块，能编译打包）
- [x] oryxos-boot 可 java -jar 运行（Spring Boot 主类）
- [x] oryxos-cli 有 main 函数，打印版本信息
- [x] Logo SVG（docs/images/oryxos-logo.svg）已放到 README
- [x] 完整项目文档 + CLAUDE.md 宪法 + `.specify/memory/constitution.md`
- [x] **第一周 spec-kit 规划闭环**：specs/001-react-runtime/ 下 spec.md（含 5 条澄清记录）、
      plan.md、research.md（R1/R1-GA/R2/R3）、data-model.md、contracts/（cli.md、config.md）、
      quickstart.md、tasks.md（69 任务）+ checklists/requirements.md（16/16）
- [x] **POM 升级 GA 线**（T001 Setup 依赖部分已落地）：Spring AI 1.1.8 GA（starter 更名
      spring-ai-starter-model-openai、core 拆分 spring-ai-model）、sqlite-jdbc 3.53.4.0、
      jsonschema 4.38.0；CVE 版本上界 log4j 2.26.1 / Tomcat 10.1.59 / swagger 2.2.47；
      enforcer snakeyaml 平局排除；全量 `mvn verify` 门禁通过
- [x] **GitHub Pages 站点上线**：https://yalearner.github.io/OryxOS-one/
      （Pages 源 = GitHub Actions；CI 动作 upload-pages-artifact v5 + deploy-pages v5）
- [x] **网站深色系改版**：force-dark + 深色调色板（bg #0b0f19 系、accent teal-cyan-indigo）；
      Logo 四版重设计（logo-dark/logo-light/favicon/README）；13 张 SVG 架构图深色重映射
- [x] 首页按钮/导航链接修复：自定义组件 href 用 withBase()；占位仓库地址替换为真实地址
- [x] **001-provider 交付并合并**（PR #1 af0fe10）：ProviderService 统一门面 + 显式映射 +
      llm_calls 审计落库 + Profile/Message/OryxTool 底座抽象；DeepSeek 冒烟真跑通
- [x] **002-react 交付并合并**（PR #2 836e740）：ReActLoop 自实现七步循环 + PromptBuilder 四段组装 +
      ToolExecutor 执行权唯一 + tool_invocations 审计 + AgentService/ProfileContext + Session/SessionManager
      最小契约 + Sandbox 接口墙 + LlmGateway 端口 + ToolSchemaAdapter 迁 core；坑一~坑八 + H4 不变量
      全有回归测试（47 tests 全绿）
- [x] **002 人工验收三项全过**：Demo 一真模型跑通（3 轮思考 + 2 次 http_get + 穿搭建议）、
      code review（无框架 Agent 封装）、oryxos.db 审计核对（llm_calls=3/tool_invocations=2 真实落库）
- [x] 人工验收方法论沉淀：`.claude/skills/oryx-spec/references/manual-acceptance.md`（临时 harness
      模板 + 六步流程 + 本机八坑）；review 指南 `docs/reviews/002-react-review.md`
- [x] **003-cli 交付并人工验收**（分支 003-cli，29 任务）：12 子命令（轻命令零 Spring 秒回 /
      重命令 chat 启动 Spring）+ CliChannel + sessions 表 JPA 持久化（SessionManager 换 JPA 实现契约不变、
      跨重启恢复铁证实跑）+ 装配 CliAgentConfiguration；坑九架构断言钉死；
      实跑修复 5 个工程缺口（init 父目录 / 嵌套 fat jar / Tomcat 抢 8080 / 建表脚本接线 / GBK 终端编码）
- [x] 003 实跑验证全过：轻命令全链、chat 中文多轮 + /quit、跨进程历史恢复、session list、
      Found 3 JPA 仓储、provider list 零 key 泄漏；review 指南 `docs/reviews/003-cli-review.md`
- [x] **003-cli 合并进 main**（PR #3 af4e610，2026-09-03）：CI security 门禁红核实为存量问题
      （spring-core 6.2.19 七个 CVE 暂无修复版本，Boot 3.5.16 已是最新），跟踪 issue #4
- [x] **004-notify 设计文档产出**（待人工复核后 /oryx-spec 开工）：六段式 + 流程图
      （docs-notify-flow.svg 浅/深双版本）；拍板：全局注册表（否决课件 Profile 字段版）、
      CRUD 归 Web 节、adapter 显式映射、唯一渠道才缺省；坑十/坑十一
- [x] **四个方案流程图齐备**：001~004 各一张 docs-*-flow.svg（浅/深双版本），内容对齐
      FR/坑对号/拍板结论；流程图约定写入 oryx-design skill（template.md「流程图约定」）
- [x] **设计流程规则固化**：课件只读不回写（docs/requirements/ 即未来课件）；冲突必须
      用户拍板且附两方案优缺点（oryx-design SKILL.md + 记忆 courseware-conflict-protocol）

## 待开发（课件分节推进，每个 feature 走 /oryx-spec 流程 + 需求文档六段式）
1. ~~第 19 节：NotifyTools + WebhookNotifyAdapter~~ ✅ 已交付（PR #5 MERGED 2026-09-05，81 tests
   + 人工验收闭环 + 40008 修复）——**留两条小尾巴**（记录于 specs/004-notify/flow-status.md
   人工验收待办 + docs/reviews/004-notify-review.md §六）：
   - [ ] 接口中立性自查（1 分钟思维练习：换企业微信官方 SDK 实现，`send(NotifyTarget, String)`
     签名应无需改动）
   - [ ] 第 20 节工具注册就位后补验"LLM 对话内自动调 notify"端到端（既定跨节契约）
2. ~~第 20 节：Tool 体系——ToolRegistry + 内置 FileTools/ShellTools/HttpTools（execute 首行
   接 Sandbox.enforce）~~ ✅ 已交付（005-tool，2026-09-05 人工验收全闭环：tool list 7 工具、
   notify 补验群收到、天气 Demo 失败重试成功、方式三 echo_tool、MCP 失联实机；Demo 一对话版
   已跑通）——**遗留两条**：① 004 接口中立性自查（用户 1 分钟思维练习）② 白名单拦截人工验证
   归 24 节；DeepSeek 工具调用 prompt 三件套见 oryx-design template §六
3. 第 21/22 节：Memory 三层——MemoryService 门面 + LongTermMemoryStore 三档 + save_memory/recall_memory
4. 第 23/24 节：WhitelistSandbox 三层白名单（路径穿越处理/命令首 token/域名通配）
5. 第 25/26 节：AgentScheduler 钟推 + Web Service 10 端点（三触发源接入 AgentService）
（每个 feature：docs/requirements/NNN-slug.md 经 /oryx-design 产出 → /oryx-spec NNN-slug 六阶段）

## 网站（VitePress + GitHub Pages，2026-08 已上线）

- 站点：https://yalearner.github.io/OryxOS-one/；源码目录 `website/`（vitepress 1.6.4，vite override 6.4.3）
- **`base: '/OryxOS-one/'` 不可改回 `/`**——子路径下资源/链接会 404（配置：`website/.vitepress/config.mts`）
- 自定义组件（Home.vue 等）内的硬编码 `href`/`src` 必须包 `withBase()`——VitePress 只自动处理
  markdown 与 config 里的链接；注意 node 侧 config 文件无 `withBase` 导出（head 条目手动带 base）
- Logo 四版：`website/public/logo-dark.svg`（深色底，站点在用）、`logo-light.svg`（浅色底）、
  `favicon.svg`、`docs/images/oryxos-logo.svg`（README 双色渐变版，GitHub 明暗模式通用）
- 架构图 SVG 均为深色调色板：早期 13 张（#101623 面板 / #1c2537 边框 / #e8edf6 文字）；
  docs-* 系列图（docs-notify 等）bg #0e1420 + 浅色标题条——新增图沿用所属系列配色；
  设计文档流程图浅/深双版本分别落 docs/images/ 与 website/public/images/
- 部署：push main 自动触发 deploy.yml；本地构建验证 `npm run docs:build`
- git push 必须走 SSH（本机 https 凭证属他人账号）：`origin`/`upstream` 均已指向
  `git@github.com:yALearner/OryxOS-one.git`，勿改回 https

## 编码约定
- 所有代码用 Java 21，充分利用 Virtual Thread
- 模块间通过接口解耦，新增 Channel/Tool 只加新模块不改 core
- 敏感配置（API key）走环境变量 ${VAR_NAME}，不明文写 YAML
- 日志用 Logback 结构化 JSON（MDC session_id + kv 字段）；**日志参数不带用户可控值**
  （sessionId/工具名等——FindSecBugs CRLF_INJECTION_LOGS 门禁会拦，关联信息在审计表）
- 遵循 CLAUDE.md 与 .specify/memory/constitution.md 的 9 条宪法原则
- 设计文档写作规则（/oryx-design）：课件只读不回写（docs/requirements/ 即未来课件）；冲突必须
  停下请用户拍板并附两方案优缺点；每份设计文档必配对齐方案描述的流程图（浅/深双版本）
- 构建：先按"实施环境"export 后再 `mvn clean package`；SQLite 测试库用 `:memory:` + pool=1
- 实施顺序以 tasks.md 的 [P] 并行标记与阶段依赖为准，每个用户故事完成后跑其 Independent Test
- 编译门禁含 Error Prone `-Werror`：`LocalDateTime.now()` 要显式时区、`split(regex)` 要 `-1` 参数、
  `toLowerCase()` 要 `Locale.ROOT`、字节转 String 要显式 UTF_8
- 手工建表（schema.sql）是唯一真相源（ddl-auto: none）——新表 DDL 增量追加，测试执行同一份脚本；
  建表脚本由 boot 启动接线（spring.sql.init.mode=always + classpath:schema.sql，SQLite 不被视为
  embedded 必须显式 always）
- 人工验收用「临时 harness」模式（真装配 + 真落库 + 真模型，@Tag("integration")），不进交付物、
  验收后删除；方法见 `.claude/skills/oryx-spec/references/manual-acceptance.md`
- CLI 实跑经验：终端输入编码跟随 `console().charset()`（硬编码 UTF-8 会把 GBK 终端读乱）；
  打包唯一入口 = oryxos-boot（mainClass=OryxOsCli，cli 模块 repackage skip 防嵌套 fat jar）；
  重命令 `web(NONE)` 不起 Tomcat；Windows 下 jar 被瞬时锁时等待数秒重试打包

## 参考文档路径
- 项目宪法：CLAUDE.md（根目录）、.specify/memory/constitution.md
- 需求文档（六段式，交付清单即对外概念白名单）：docs/requirements/001-provider.md、002-react.md、
  003-cli.md、004-notify.md（每份必配对齐方案描述的流程图 docs-*-flow.svg 浅/深双版本，
  约定见 .claude/skills/oryx-design/references/template.md「流程图约定」）
- spec-kit 流程工件：specs/001-provider/、specs/002-react/、specs/003-cli/（spec/plan/research/
  data-model/contracts/quickstart/tasks/checklists/flow-status）
- 流程与验收方法论：.claude/skills/oryx-spec/（references/gates.md、manual-acceptance.md、
  requirements-convention.md）、.claude/skills/oryx-design/（六段式需求设计模板）
- 代码 review 指南：docs/reviews/001-provider-review.md、002-react-review.md、003-cli-review.md、004-notify-review.md、005-tool-review.md
- 业界调研：docs/IndustryResearch.md；需求分析：docs/DemandAnalysis.md
- 技术方案：docs/TechnicalSolution.md；AI 编程指南：docs/AiProgrammingGuide.md
- 历史规划工件（旧序列，不计入新编号）：specs/001-react-runtime/
```
