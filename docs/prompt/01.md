# OryxOS — 对话提示词记录

## 1
帮我清楚项目的九个模块

## 2
不用备份帮我删除

## 3
根据项目当前docs目录下的四个文档 以及claude.md 帮我用maven初始化这个项目，按照文档中的9个模块，并能通过编译打包 只初始化模块不要写业务代码

## 4
boot目录现在能不能 java -jar运行了吗？ 是一个main函数

## 5
oryxos 的cli 需要一个main函数，他要能运行 打印oryxos的版本信息

## 6
帮我画一个oryxos的logo,放在readme,用svg格式，自由发挥 我需要一个logo

## 7
帮我在优化一下这个logo 在简介大方一点 建议去掉下面文字

## 8
logo再小一点 并且左边的图像换一个简介大方的

## 9
帮我将logo放到readme.md中

## 10
能帮我再次优化logo将图案和文件结合起来

## 11
给我打印出我前面给你的所有的提示词，放到prompt目录下面

## 12
将这些提示词打印到D:\myproject\OryxOS-one\docs\prompt目录下 01.md中这是当前全部的提示词 帮我生成一个可以让loop编程理解的提示词

---

## Loop 编程专用提示词（AI Agent 上下文注入）

> 以下提示词可直接注入给 AI 编程 Loop（如 Claude Code / Cursor / Copilot），使其快速理解 OryxOS 项目全貌并继续开发。

```markdown
# OryxOS 项目上下文

## 项目定位
OryxOS 是用 Java 21 + Spring Boot 3.x 实现的面向企业场景的 Agent OS（Agent 统一底座）。
装在企业自己的 K8s 或服务器上，作为统一底座运行多个业务 Agent，共享渠道接入、模型路由、
工具调用、记忆系统、沙箱执行能力。数据完全留在企业自己的基础设施，不锁任何云生态。
Agent 在 OryxOS 上是**配置出来的，不是写代码写出来的**。

## 当前阶段
**核心阶段（pre-alpha）**：单机私有部署，运行时内核开发中。已完成 Maven 多模块初始化、
编译打包通过、CLI 版本打印、Logo 设计、README 架构图内联。

## 9 个 Maven 模块（已初始化，无业务代码）

| 模块 | 职责 |
|------|------|
| `oryxos-core` | 核心抽象：ReActLoop、PromptBuilder、ToolExecutor、AgentService、AgentScheduler |
| `oryxos-provider` | LLM Provider 抽象，多 Provider 显式映射（Map<name, ChatModel>） |
| `oryxos-memory` | MemoryService 统一门面，LongTermMemory 可插拔后端 |
| `oryxos-tool` | 内置 Tool、MCP Client、ToolRegistry、Sandbox、NotifyTools（四合一） |
| `oryxos-channel-cli` | CLI Channel 交互式对话 |
| `oryxos-web` | REST API（10 个端点）、GlobalExceptionHandler、OpenAPI |
| `oryxos-storage` | SQLite 持久化、审计表（tool_invocations + llm_calls）写入 |
| `oryxos-cli` | Picocli 命令行入口（12 个子命令）、ConfigLoader |
| `oryxos-boot` | Spring Boot 启动模块、自动配置、依赖聚合 |

**依赖方向**：storage → core → provider/memory/tool → web/channel-cli → cli → boot

## 关键设计决策（不可违背）

1. **ReAct Loop 自实现**：不用 Spring AI 的 Agent 抽象，自己写核心循环
2. **Spring AI 只用两件事**：LLM Provider 协议转换 + @Tool JSON Schema 生成。禁用自动 tool 执行
3. **Provider 显式映射**：Map<String, ChatModel>，不靠类型扫描
4. **一个目录 = 一个 Agent**：.oryxos/agents/<name>/AGENT.md（frontmatter + 正文）
5. **审计 Day One**：tool_invocations + llm_calls 两张表从核心阶段就写入 SQLite
6. **同步执行模型**：全程同步阻塞 + Java 21 Virtual Thread，不引入 Reactor/WebFlux
7. **Skill 渐进式披露**：L1 元数据注入 → L2 read_file 读 SKILL.md → L3 按需读附属资源
8. **三种触发源共用一个引擎**：CLI/Web/Scheduler → AgentService.process() → ReActLoop
9. **Sandbox 接口先行**：只暴露 enforce(SandboxAction)，不携带实现细节

## 技术栈
- Java 21 + Spring Boot 3.x + Maven
- Spring AI Alibaba（LLM 协议转换）
- Picocli（CLI）、SQLite + JPA（持久化）、Logback（日志）
- MCP（Model Context Protocol，Tool 协议）
- 核心阶段无需 PostgreSQL、Redis 等外部依赖

## 工作区结构（运行时）
```
.oryxos/
├── agents/        # 每个子目录 = 一个 Agent（AGENT.md + skills/ + scripts/）
├── skills/        # 公共 Skill 实体库
├── memory/
│   └── MEMORY.md  # 长期记忆
├── sessions/      # 会话数据
├── logs/          # 结构化日志
├── mcp_servers.yaml
├── oryxos.db      # SQLite
├── AGENTS.md / SOUL.md / USER.md  # Bootstrap 文件
```

## 当前已完成的工作
- [x] Maven 多模块项目初始化（9 个模块，能编译打包）
- [x] oryxos-boot 可 java -jar 运行（Spring Boot 主类）
- [x] oryxos-cli 有 main 函数，打印版本信息
- [x] Logo SVG（docs/images/oryxos-logo.svg）已放到 README
- [x] README 架构图内联为 SVG 原图（不再引用外部文件）
- [x] 完整项目文档：IndustryResearch.md / DemandAnalysis.md / TechnicalSolution.md / AiProgrammingGuide.md / CLAUDE.md

## 待开发的核心功能（按优先级）
1. Provider 抽象 + ReAct Loop（第一周核心任务）
2. Memory + Tool 体系（第二周）
3. Web Service + 10 个 REST 端点（第三周）
4. 多 Agent 演示 + 工程化收尾（第四周）

## 编码约定
- 所有代码用 Java 21，充分利用 Virtual Thread
- 模块间通过接口解耦，新增 Channel/Tool 只加新模块不改 core
- 敏感配置（API key）走环境变量 ${VAR_NAME}，不明文写 YAML
- 日志用 Logback 结构化 JSON 格式
- 遵循 CLAUDE.md 中的 9 条 Constitution 原则
- 使用 `mvn clean package -DskipTests` 编译打包

## 参考文档路径
- 项目宪法：CLAUDE.md（根目录）
- 业界调研：docs/IndustryResearch.md
- 需求分析：docs/DemandAnalysis.md
- 技术方案：docs/TechnicalSolution.md
- AI 编程指南：docs/AiProgrammingGuide.md
```
