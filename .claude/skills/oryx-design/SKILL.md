---
name: oryx-design
description: >-
  生成 OryxOS 模块设计文档（docs/requirements/NNN-slug.md）：输入目标模块（US-n 或能力名），
  以 docs/ 四份文档（AiProgrammingGuide / DemandAnalysis / IndustryResearch / TechnicalSolution）
  为权威设计源、课程课件（docs/class/ 各节，可指定路径）为实施级事实源，按六段架构成文——
  背景与价值 → 用户场景 → 功能需求（FR/NFR 表，交付物列即白名单）→ 明确不做 → 验收标准
  （自动化指向验收 harness，人工指向"做完怎么验"）→ 依赖与假设（指向前序课交付物，须实测
  代码现状）。沿用 001-provider / 002-react 事实模板的全部约定。每份设计文档必配对齐方案描述
  的流程图（浅/深双版本）。课件只读不回写（设计文档即未来课件）；课件与四文档冲突时停下报告
  等用户拍板（附两方案优缺点），不得自行仲裁。
  当用户说「生成设计文档 / 下一课需求 / 下一个模块设计文档」时使用。
argument-hint: "<目标模块：US-n | 能力名>"
user-invocable: true
disable-model-invocation: false
---

# oryx-design：设计文档生成

OryxOS 主体开发每节课（一个核心能力）开工前的上游步骤：把四份 docs 文档转成该模块的
设计文档，作为 `/oryx-spec` 的需求输入。已产出的两份——`docs/requirements/001-provider.md`
（US-1）、`docs/requirements/002-react.md`（US-2）——是本 skill 的**事实模板**，
后续 US-3/4/5 沿用同一约定。

本文件只描述流程骨架；六段模板与写作约定按需读取 `references/template.md`（渐进式披露）。

## 0. 参数解析

- **目标模块**：`US-n`（n=1..5）或能力名（如"Memory""Web Service"）。
- **NNN-slug**：NNN = `docs/requirements/` 下**新序列**下一个可用序号（新序列从
  `001-provider` 起，历史产物 `specs/001-react-runtime` 不计入，见 oryx-spec 的
  `references/requirements-convention.md`）；slug = 能力短名（小写连字符），
  与 `/oryx-spec` 的 `specs/NNN-slug/` 一一对应。机械可判，不猜。
- **四文档章节索引**：按 `references/template.md` 的映射表定位。映射只是索引，
  正文永远以**实读四文档**为准（单一真相源，杜绝双份漂移）。
- **课件定位**：课程课件（course repo `docs/class/`，用户指定其他路径则用之）是**实施级事实源**——
  代码骨架、已定文案、测试类清单、约定条目、分节归属；四文档是**权威设计源**。课件不是
  四文档的替代，而是"实际踩过坑的落地方案"。
- **课件只读不回写**（用户 2026-09-04 指示）：course repo 课件是历史教学材料，**不得修改**；
  维护重心只在当前项目——`docs/requirements/` 设计文档**就是未来的课件**，成文必须自足完整
  （冲突、拍板结论、方案取舍理由都写入正文，供未来学员学习）。

## 1. 生成流程（六步）

1. **解析与简报**：确定 NNN-slug + 对应 US + 章节索引，**落笔前向用户简报一次**
   （编号、范围、章节索引），确认后继续——文件名与编号是后续 /oryx-spec 的契约，不静默定。
2. **读事实模板**：`docs/requirements/001-provider.md`（结构/表格/文风基准），
   `docs/requirements/002-react.md` 作第二个示例。
3. **读四文档相关章节全文 + 对应课件节全文**（如存在）：四文档是权威设计源，课件提供
   实施级事实（签名骨架 / 文案 / 测试类清单 / 约定）。不引入 specs/ 产物或其他架构事实。
4. **核对代码现状（必做）**：前序课交付物是否如文档所述（类/表/配置键逐一核实），
   本课相关模块是否空壳——用文件列表 / schema.sql / git log 实测，不凭文档假设。
   发现不符 → 停下报告（见 §2 停止清单第 3 条）。
5. **六段成文**：按 `references/template.md` 的模板与写作约定落笔——含「本节交付物清单」
   （代码/测试/表/约定四类）、坑的编号命名与测试对号、核心代码骨架、最值钱的回归测试示例代码；
   **并产出方案流程图**（每份设计文档必配一张，内容必须对齐方案描述、双版本落位与引用方式见
   template.md「流程图约定」）。
6. **完成报告**：产物路径（含流程图两版本文件路径）+ 「为实现级明确」决策清单 + 超出四文档的
   新增对外概念（如有）+ 冲突拍板记录（课件 vs 四文档，如有）+ 前序缺口（H0 依赖检查发现，如有）
   + 下一步 `/oryx-spec NNN-slug`。

## 2. 判断权与停止清单

- **机械可判的自行完成**：编号取号、章节索引、代码现状核对、模板结构、措辞风格。
- **需要判断的按「为实现级明确」标记**：超出四文档字面表述的语义补充（补一个默认值、
  明确一个边界行为）、四文档之间的非宪法冲突（默认按较新文档取舍：技术方案 > 需求文档 >
  其他）。标记后写入正文并进入完成报告，由用户复核——不隐瞒、不写死。
- **课件与四文档（或宪法）冲突 → 停下报告，不得自行仲裁**（Harness H1 同款纪律）：
  列出冲突点清单（课件说法 vs 文档说法），**必须同时给出两种方案的当前优缺点对比**（用户
  2026-09-04 指示），用 AskUserQuestion 等用户拍板；拍板结论写入文档头部「修订说明」，并在
  完成报告中记录。历史案例：002-react 经用户拍板"冲突一律参照课件"（唯一例外：课件 skills
  字段表述 vs 宪法 IV 软连接，按宪法）；004-notify 经用户拍板"按技术方案 §6.8 全局注册表"
  （课件 Profile 字段版为旧版，附两方案优缺点对比表后拍板）。
- **停下并报告（用 AskUserQuestion 等确认，不得自行绕过）**：
  1. 需要引入四文档与课件之外的对外概念（新表 / 新配置键 / 新类 / 新模块）
  2. 涉及宪法 9 条原则的文档冲突（宪法 supersede 一切，见 `.specify/memory/constitution.md`）
  3. 前序交付物实测与文档描述不符
  4. 用户已明确的结构要求与本模板冲突

## 3. 与 oryx-spec 的衔接

产出的 `docs/requirements/NNN-slug.md` 是 `/oryx-spec NNN-slug` 的需求输入（oryx-spec
编号解析的第一形态）。两份文档的对应关系：

- 设计文档的**交付物列** = oryx-spec 交付清单白名单（停止清单第 1 条的判断基准）
- 设计文档的**验收标准** = oryx-spec S4 tasks 的 acceptance criteria 来源
- 设计文档的**依赖与假设（跨节契约）** = oryx-spec 停止清单第 4 条"前序公共接口"的判断依据
